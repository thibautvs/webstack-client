- configure gulp
  * minify html
  * HTML report for unit and e2e tests (logs folder)
  * configure protractor with mocha + test sinon
  * only minify + bundle if production, not dev + fingerprint bundles
  * watcher for JSHint
  * make sure clean task is synchronous (use it as task dependency in the main bundling file doing HTML/CSS/JS)
  * update gulp-ruby-sass (sourcemap not working, wrong path)
- i18n and l10n
- call to ReST API
- auth

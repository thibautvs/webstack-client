- configure gulp
  * minify html
  * HTML report for unit and e2e tests (logs folder)
  * configure protractor with mocha + test sinon
  * only minify + bundle if production, not dev + fingerprint bundles
  * watcher for JSHint
  * source maps for SASS (only dev mode)
  * make sure clean task is synchronous (or add as dependency)
  ? live reload
  ? image optimization (gulp-image-optimization)
  ? auto CSS prefixing (gulp-autoprefixer)
  ? investigate gulp-ng-annotate
  ? investigate gulp-changed
- i18n and l10n
- call to ReST API
- auth
